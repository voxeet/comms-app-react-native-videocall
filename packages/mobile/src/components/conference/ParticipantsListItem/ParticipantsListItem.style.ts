import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginTop: 16,
    marginBottom: 16,
    alignItems: 'center',
    justifyContent: 'space-between',
  },

  iconAndtext: {
    flexDirection: 'row',
    alignItems: 'center',
    flexShrink: 1,
  },
});
