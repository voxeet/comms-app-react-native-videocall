import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    padding: 10,
    borderRadius: 10,
    borderWidth: 1,
    marginTop: 20,
    borderColor: 'white',
  },
});

export default styles;
