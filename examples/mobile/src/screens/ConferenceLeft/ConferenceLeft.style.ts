import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  text: {
    paddingTop: 100,
    paddingBottom: 24,
  },
  gap: {
    paddingTop: 16,
  },
});
export default styles;
