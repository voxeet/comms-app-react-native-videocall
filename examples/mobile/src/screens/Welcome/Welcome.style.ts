import {StyleSheet} from 'react-native';

const styles = () =>
  StyleSheet.create({
    wrapper: {
      flex: 1,
      flexDirection: 'column',
    },
    content: {
      margin: 15.63,
      flexWrap: 'wrap',
      alignContent: 'center',
    },
  });
export default styles;
