import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  infoIconWrapper: {
    position: 'absolute',
    right: 0,
  },
});
export default styles;
